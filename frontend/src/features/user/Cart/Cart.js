import React from 'react';
import styled from 'styled-components';

const CartContainer = styled.div`
  padding: 2rem;
  text-align: center;
`;

const Cart = () => (
  <CartContainer>
    <h2>Giỏ hàng</h2>
    <p>Chức năng giỏ hàng (placeholder)</p>
  </CartContainer>
);

export default Cart;