import React from 'react';
import styled from 'styled-components';

const WishlistContainer = styled.div`
  padding: 2rem;
  text-align: center;
`;

const Wishlist = () => (
  <WishlistContainer>
    <h2>Danh sách yêu thích</h2>
    <p>Chức năng wishlist (placeholder)</p>
  </WishlistContainer>
);

export default Wishlist;