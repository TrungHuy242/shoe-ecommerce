import React from 'react';
import styled from 'styled-components';

const LoginContainer = styled.div`
  padding: 2rem;
  text-align: center;
`;

const Login = () => (
  <LoginContainer>
    <h2>Đăng nhập</h2>
    <p>Chức năng đăng nhập (placeholder)</p>
  </LoginContainer>
);

export default Login;