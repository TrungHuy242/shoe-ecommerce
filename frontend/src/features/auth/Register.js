import React from 'react';
import styled from 'styled-components';

const RegisterContainer = styled.div`
  padding: 2rem;
  text-align: center;
`;

const Register = () => (
  <RegisterContainer>
    <h2>Đăng ký</h2>
    <p>Chức năng đăng ký (placeholder)</p>
  </RegisterContainer>
);

export default Register;